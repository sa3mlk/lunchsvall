# Simulate a visitor at 00:05 to get a fresh scrape result.
5 0 * * * curl http://localhost/lunch/
# Refresh the cache at 08:00.
0 8 * * * python /home/jonasg/code/lunchsvall/refresh.py && curl http://localhost/lunch/

